<vte:include
	file="{VIVVO_FS_ADMIN_DIR}templates/gui/html_{gui_element}.xml"
	label="{label}"
	prefix="{prefix}"
	maxlength="{maxlength}"
	name="{name}"
	value="{value}"
	id="{id}"
	style="{style}"
	sufix="{sufix}"
	tooltip="{tooltip}"
	input_id="{input_id}"
	link_id="{link_id}"
	checked="{checked}"
	options="{options}"
	size="{size}"
	show_none="{show_none}"
	show_all="{show_all}"
	dir="{dir}"
	fs_root="{fs_root}"
	ext="{ext}"
	name_value="{name_value}"
	value_1="{value_1}"
	value_2="{value_2}"
	text_1="{text_1}"
	text_2="{text_2}"
	items="{items}"
	noscript="{noscript}"
	feedurl="{feedurl}"
	topic_id="{topic_id}"
	default_topic_name="{default_topic_name}"
	action_handler="{action_handler}"
/>